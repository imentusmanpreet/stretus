# ── Web Framework ──────────────────────────────────────────
fastapi[standard]==0.115.0
uvicorn[standard]==0.30.6

# ── Database ───────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3

# ── Validation & Settings ──────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ── AI / NLP ───────────────────────────────────────────────
ollama==0.3.3
openai==1.54.3
groq==0.11.0

# ── YAML & Utilities ───────────────────────────────────────
pyyaml==6.0.2
python-multipart==0.0.12
httpx==0.27.2
# orjson is used by quant_engine_client to encode 1M+ row OHLCV POSTs
# without building a giant intermediate Python str. ~3-10× faster than
# stdlib json for this workload and uses noticeably less peak memory.
orjson==3.10.7
grpcio==1.80.0
protobuf>=5.26.1,<7

# ── Dev / Testing ──────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
requests==2.32.3

# ── Data & Trading ─────────────────────────────────────────
yfinance==0.2.44
pandas==2.2.3
numpy==1.26.4
openpyxl>=3.1.2,<4

# TA-Lib — official Python wrapper for the TA-Lib C library.
# Provides 150+ indicators + 60+ candlestick patterns, C-speed (2-4x faster than SWIG).
# Requires the TA-Lib C library installed on the host — handled in Dockerfile.
TA-Lib==0.6.8

# NSE India data
nsepython==2.9

# ── UI / Streamlit ─────────────────────────────────────────
streamlit>=1.32,<2