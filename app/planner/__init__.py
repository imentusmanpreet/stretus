"""
app/planner — Strategy generation pipeline (v2).

Replaces app/services/knowledge/retriever.py.

Public API:
    from app.planner.pipeline import Pipeline, plan_strategy
    from app.planner.adapters import plan_to_strategy_config
"""
